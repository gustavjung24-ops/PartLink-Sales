node_modules

# Build outputs
dist/
release/

# Environment files (contain secrets)
.env
.env.local
.env.*.local

# Code signing certificates — NEVER commit to git
# Store securely in a password manager or CI secrets
**/certs/*.p12
**/certs/*.pfx
**/certs/*.cer
**/certs/*.key
certs/
